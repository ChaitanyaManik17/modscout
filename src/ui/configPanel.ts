// src/ui/configPanel.ts
// The mod configuration panel is handled via Devvit.createForm in main.tsx.
// This file exports a no-op to keep the import in main.tsx clean.

export function renderConfigPanel(): void {
  // Configuration is managed through Devvit.createForm (see main.tsx).
  // This export exists for future UI expansion.
}
